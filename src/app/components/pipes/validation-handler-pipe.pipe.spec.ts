import { ValidationHandlerPipePipe } from './validation-handler-pipe.pipe';

describe('ValidationHandlerPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidationHandlerPipePipe();
    expect(pipe).toBeTruthy();
  });
});
